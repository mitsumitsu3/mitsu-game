{
  "name": "mitsu-game-resolver",
  "version": "1.0.0",
  "description": "Lambda Resolver for Mitsu Game AppSync API",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/lib-dynamodb": "^3.0.0",
    "uuid": "^9.0.0"
  }
}
